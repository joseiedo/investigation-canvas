---
phase: 08-board-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - BOARD-03

must_haves:
  truths:
    - "Dragging the board background pans the entire workspace"
    - "Using the mouse wheel zooms the board in and out"
    - "Zooming is centered on the mouse cursor"
    - "Cards remain draggable and correctly track the mouse at any zoom level"
    - "SVG connections correctly track card centers at any zoom level"
  artifacts:
    - path: "index.html"
      provides: "Transform container (#world) for panning and zooming"
      contains: "id="world""
    - path: "index.html"
      provides: "Pan and zoom interaction logic"
      contains: "addEventListener('wheel')"
---

<objective>
Implement board panning and zooming to allow for a scalable investigation workspace.

Purpose: Break the limitation of the single viewport. Panning and zooming allow the user to manage a larger board, focus on details, and organize evidence more effectively.
Output: Updated index.html with a transformable #world container and navigation logic.
</objective>

<execution_context>
@/Users/iedo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iedo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-board-navigation/08-CONTEXT.md
@.planning/phases/08-board-navigation/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure HTML and CSS for #world container</name>
  <files>index.html</files>
  <action>
1. Wrap all `.card` elements and the `#connections-layer` SVG inside a new `<div id="world">` element.
2. Update CSS to handle the transformable world.

**HTML update:**
```html
<div id="board">
  ... buttons ...
  <div id="world">
    <svg id="connections-layer">...</svg>
    ... cards ...
  </div>
</div>
```

**CSS update:**
```css
    #world {
      position: absolute;
      inset: 0;
      transform-origin: 0 0;
      pointer-events: none; /* So we can click the board background to pan */
    }

    #world > * {
      pointer-events: auto; /* Re-enable events for cards and SVG elements if needed */
    }
```
  </action>
</task>

<task type="auto">
  <name>Task 2: Implement Pan/Zoom and Refactor Drag Logic</name>
  <files>index.html</files>
  <action>
Update the `<script>` block to implement the navigation logic and fix coordinate scaling.

**Key logic to implement:**
1. **Pan State**: `panX`, `panY`, `isPanning`.
2. **Zoom State**: `zoom`.
3. **Wheel Listener**: Update `zoom` and `panX/Y` to zoom into the focal point.
4. **Board Mousedown**: Detect click on background to start panning.
5. **Drag Refactor**: Use `e.movementX / zoom` to move cards in world space.
6. **Connection Refactor**: Use `card.offsetLeft + card.offsetWidth/2` (world coords) instead of `getBoundingClientRect()` (scaled screen coords) for `updatePath`.

**Coordinate mapping snippet:**
```javascript
function updateWorldTransform() {
  world.style.transform = `translate(${panX}px, ${panY}px) scale(${zoom})`;
}
```
  </action>
</task>

</tasks>

<verification>
1. Open `index.html`.
2. Drag the background (not a card). Verify all cards move with the mouse.
3. Use the scroll wheel. Verify the board scales up/down centered on your mouse.
4. Drag a card while zoomed way out. Verify it follows the mouse perfectly.
5. Draw a connection while zoomed in. Verify it stays attached to card centers.
6. Reset the board. Verify navigation state also resets (or stays, either is fine).
</verification>

<success_criteria>
1. Background dragging pans the world.
2. Scroll wheel zooms the world.
3. Zoom is focal-point aware (zooms into cursor).
4. Card dragging is scale-aware (no lag or overshooting).
5. SVG connections track correctly at any scale.
</success_criteria>

<output>
After completion, create `.planning/phases/08-board-navigation/08-01-SUMMARY.md` with:
- Implementation details (Transform strategy, zoom focal point math).
- Verification results.
</output>
