---
phase: 03-svg-connections
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified: []
autonomous: false
requirements:
  - CONN-01
  - CONN-02
  - CONN-03
  - CONN-04

must_haves:
  truths:
    - "User has verified that 'Connect Mode' can be toggled on and off"
    - "User has verified that clicking two cards in connect mode creates a red string"
    - "User has verified that the string has a physical-feeling sag"
    - "User has verified that the string follows the card centers during drag"
  artifacts:
    - path: "index.html"
      provides: "Verified SVG connection board — confirmed by human interaction"
---

<objective>
Human interaction verification of the red string SVG connections.

Purpose: The specific gravity feel of the "sag" and the smoothness of the dynamic line updates are critical aesthetic elements that require human verification.
Output: Confirmed Phase 3 complete — or issues reported for gap closure.
</objective>

<execution_context>
@/Users/iedo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iedo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-svg-connections/03-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Interaction verification of SVG connections</name>
  <what-built>
Plan 01 implemented red string SVG connections in index.html with:
- "Connect Mode" toggle button (floating at top-left)
- Red Quadratic Bezier curves for a physical sagging feel
- Center-to-center line logic
- Real-time updates during card dragging
  </what-built>
  <action>
Open index.html in your browser and verify all 5 connection checks:

1. **Connect Mode Toggle** — Click the button at the top-left. It should change from "OFF" (red/brown) to "ON" (bright red). Card cursors should change to `crosshair`.

2. **Drawing Strings** — In "ON" state, click any two cards sequentially. A red line should appear between their centers.

3. **String Sag** — The line should not be perfectly straight; it should curve slightly downwards, like a physical string under gravity.

4. **Real-time Dragging** — Drag a connected card. The red string must stay perfectly attached to its center and update its curve as you move the card.

5. **Multiple Connections** — Draw multiple lines (e.g., A-B, B-C, A-D). Verify they all persist and correctly update when their respective cards are dragged.

6. **Toggle Off** — Switch "Connect Mode" back to "OFF". Verify you can drag cards normally again and that the strings remain in place.
  </action>
  <verify>
    <automated>MISSING — interaction feel cannot be automated; human testing required</automated>
    <manual>Open index.html in browser and verify all 6 connection checks listed above</manual>
  </verify>
  <done>User types "approved" confirming all 6 connection checks pass. Phase 3 is complete.</done>
  <resume-signal>Type "approved" if the connections feel correct. Describe any issues found (e.g., "lines are straight", "lines don't update", "cannot toggle mode") if anything needs fixing.</resume-signal>
</task>

</tasks>

<verification>
Human interaction testing as described in the checkpoint task above.
</verification>

<success_criteria>
User approves the interaction. All 6 interaction checks pass. Phase 3 is complete.
</success_criteria>

<output>
After user approval, create `.planning/phases/03-svg-connections/03-02-SUMMARY.md` with:
- Confirmation that interaction verification passed
- Phase 3 marked complete
</output>
