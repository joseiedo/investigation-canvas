---
phase: 03-svg-connections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - CONN-01
  - CONN-02
  - CONN-03
  - CONN-04

must_haves:
  truths:
    - "A visible 'Connect Mode' button exists and toggles the connection behavior"
    - "In connect mode, clicking two cards draws a visible red line between them"
    - "Red lines sag slightly (curved) to simulate physical string"
    - "Dragging a connected card updates the string position in real-time"
    - "Multiple strings can exist on the board simultaneously"
  artifacts:
    - path: "index.html"
      provides: "SVG overlay container for connection lines"
      contains: "<svg"
    - path: "index.html"
      provides: "Connect Mode toggle button and UI state"
      contains: "id="toggle-connect""
    - path: "index.html"
      provides: "Dynamic line update logic inside drag loop"
      contains: "updateConnections"
---

<objective>
Implement red string connections between evidence cards using a dynamic SVG overlay.

Purpose: Deliver the "conspiracy wall" payoff — the ability to visually link evidence. This requires integrating a persistent drawing layer with the existing drag-and-drop system.
Output: Updated index.html with "Connect Mode" UI, SVG drawing layer, and real-time line update logic.
</objective>

<execution_context>
@/Users/iedo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iedo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-svg-connections/03-CONTEXT.md
@.planning/phases/03-svg-connections/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SVG layer and Connect UI to index.html</name>
  <files>index.html</files>
  <action>
1. Add a `<button id="toggle-connect">Connect Mode: OFF</button>` inside the `#board`.
2. Add a `<svg id="connections-layer">` inside the `#board`, before the cards.
3. Add CSS for the button and the SVG layer.

**CSS to add:**
```css
    #toggle-connect {
      position: absolute;
      top: 20px;
      left: 20px;
      z-index: 100;
      padding: 10px 16px;
      background: #922b21;
      color: white;
      border: none;
      border-radius: 4px;
      font-family: 'Courier New', Courier, monospace;
      font-weight: bold;
      cursor: pointer;
      box-shadow: 0 4px 6px rgba(0,0,0,0.3);
      transition: background 0.2s;
    }

    #toggle-connect.active {
      background: #e74c3c;
      box-shadow: 0 0 10px rgba(231, 76, 60, 0.5);
    }

    #connections-layer {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      pointer-events: none;
      z-index: 15; /* Above vignette (10) but below cards (20+) */
    }

    #board.connect-mode .card {
      cursor: crosshair;
    }
```
  </action>
</task>

<task type="auto">
  <name>Task 2: Implement Connection Logic</name>
  <files>index.html</files>
  <action>
Update the `<script>` block to handle connect mode, line drawing, and real-time updates.

1. Add state variables: `isConnectMode`, `selectedCard`, `connections`.
2. Add a click handler for the toggle button.
3. Update the card `mousedown` (or add a separate `click` handler) to handle card selection in connect mode.
4. Add a `drawConnection(from, to)` function that creates an SVG `<path>`.
5. Add an `updateConnections()` function that recalculates path `d` attributes using Quadratic Bezier formulas.
6. Call `updateConnections()` inside `onMouseMove`.

**Logic for Quadratic Bezier (`Q`):**
```javascript
function getPathD(x1, y1, x2, y2) {
  const midX = (x1 + x2) / 2;
  const midY = (y1 + y2) / 2 + 30; // 30px sag
  return `M ${x1} ${y1} Q ${midX} ${midY} ${x2} ${y2}`;
}
```
  </action>
</task>

</tasks>

<verification>
1. Open `index.html` and verify the "Connect Mode" button is visible.
2. Toggle the button; check that the button color changes and card cursors change to `crosshair`.
3. In connect mode, click Card A then Card B. A red line should appear.
4. Drag either Card A or Card B; the line must stay attached to the card centers and update its curve in real-time.
5. Draw multiple lines; verify they all update independently during drag.
</verification>

<success_criteria>
1. "Connect Mode" button works and provides visual feedback.
2. Clicking two cards in connect mode creates a red string line.
3. Lines have a visible "sag" (curved path).
4. Lines update dynamically during card dragging.
5. Connecting the same two cards twice is allowed (or prevented, either is fine for PoC).
</success_criteria>

<output>
After completion, create `.planning/phases/03-svg-connections/03-01-SUMMARY.md` with:
- Implementation details (SVG path logic, toggle state management).
- Verification results.
</output>
