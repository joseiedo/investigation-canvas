---
phase: 09-investigation-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - CARD-03
  - CONN-05

must_haves:
  truths:
    - "Card titles can be edited in the expanded view and update the board card in real-time"
    - "Right-clicking a red string connection deletes it immediately"
    - "Renamed titles persist after browser refresh"
    - "Deleted connections remain deleted after browser refresh"
  artifacts:
    - path: "index.html"
      provides: "Editable title input in the expanded view"
      contains: "<input id="dialog-title-input""
    - path: "index.html"
      provides: "Right-click connection deletion logic"
      contains: "contextmenu"
---

<objective>
Implement card renaming and connection deletion to provide full investigation management capabilities.

Purpose: Allow the investigation board to evolve. Users can refine their findings by renaming evidence and prune their theories by removing outdated or incorrect connections.
Output: Updated index.html with editable titles and deletable connections.
</objective>

<execution_context>
@/Users/iedo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iedo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-investigation-management/09-CONTEXT.md
@.planning/phases/09-investigation-management/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add editable title input and styles to index.html</name>
  <files>index.html</files>
  <action>
1. Replace `<h1 id="dialog-title"></h1>` with `<input id="dialog-title-input" type="text">` in the `<dialog>`.
2. Update CSS to style the input like a typewriter title.

**HTML update:**
```html
<input id="dialog-title-input" type="text" placeholder="Enter Evidence Name...">
```

**CSS update:**
```css
    #dialog-title-input {
      width: 100%;
      background: transparent;
      border: none;
      border-bottom: 2px solid #2a1f14;
      font-family: 'Courier New', Courier, monospace;
      font-size: 24px;
      font-weight: bold;
      text-transform: uppercase;
      color: #1a1a1a;
      margin-bottom: 20px;
      padding-bottom: 10px;
      outline: none;
    }
```
  </action>
</task>

<task type="auto">
  <name>Task 2: Implement Renaming and Deletion Logic</name>
  <files>index.html</files>
  <action>
Update the `<script>` block to handle title persistence and connection deletion.

**Key logic:**
1. **Title Persistence**: Initialize `cardTitles` from `localStorage`.
2. **Rename Event**: Add an `input` listener to `#dialog-title-input`. Update `cardTitles[currentCardIndex]` and the board card's `.card-title`.
3. **Deletion Event**: In `createConnection`, add a `contextmenu` listener to the SVG `<path>`.
   - Prevent default context menu.
   - Remove path from DOM.
   - Splice connection from `connections` array.
   - Call `save()`.
4. **SVG Interactive Fix**: Set `pointer-events: visibleStroke` on the `<path>` elements.

**Connection deletion snippet:**
```javascript
p.style.pointerEvents = 'visibleStroke';
p.style.cursor = 'pointer';
p.addEventListener('contextmenu', (e) => {
  e.preventDefault();
  if (confirm('Delete this connection?')) {
    p.remove();
    const idx = connections.findIndex(c => c.path === p);
    if (idx > -1) connections.splice(idx, 1);
    save();
  }
});
```
  </action>
</task>

</tasks>

<verification>
1. Open `index.html`. Open "Suspect A".
2. Rename it to "Arthur Miller". Close modal. Verify board card says "Arthur Miller".
3. Refresh page. Verify "Arthur Miller" persists.
4. Draw a red string between two cards.
5. Right-click the red string. Confirm deletion. Verify string is gone.
6. Refresh page. Verify string is still gone.
</verification>

<success_criteria>
1. Card titles are fully editable and persistent.
2. Connection lines can be deleted via right-click with a confirmation.
3. Persistence engine handles the new state (titles and connection array changes).
</success_criteria>

<output>
After completion, create `.planning/phases/09-investigation-management/09-01-SUMMARY.md` with:
- Implementation details (Input synchronization, SVG event targeting).
- Verification results.
</output>
