---
phase: 09-investigation-management
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified: []
autonomous: false
requirements:
  - CARD-03
  - CONN-05

must_haves:
  truths:
    - "User has verified that card renaming updates the board in real-time"
    - "User has verified that right-clicking a string deletes it"
    - "User has verified that all changes persist after refresh"
  artifacts:
    - path: "index.html"
      provides: "Verified investigation management — confirmed by human interaction"
---

<objective>
Human interaction verification of renaming and deletion features.

Purpose: Verifying the "evolution" of the board requires a human to confirm that renaming and deleting feel intuitive and that the persistence is reliable.
Output: Confirmed Phase 9 complete.
</objective>

<execution_context>
@/Users/iedo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iedo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-investigation-management/09-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Interaction verification of investigation management</name>
  <what-built>
Plan 01 implemented management tools in index.html with:
- Editable title input in the expanded modal
- Real-time synchronization between modal title and board card title
- Right-click (`contextmenu`) deletion for SVG red strings
- Persistence for custom titles and connection removals
  </what-built>
  <action>
Open index.html in your browser and verify these 3 management checks:

1. **Renaming** — Open a card (e.g., "Suspect A"). Click the title and change it to a character name (e.g., "John Doe"). Look at the board card while the modal is open (or after closing); it should update to "John Doe" immediately.

2. **Connection Deletion** — Ensure you have a red string between two cards. Right-click directly on the red line. A "Delete this connection?" prompt should appear. Confirm it, and the string should disappear.

3. **Persistence** — Refresh the page (Cmd+R). Verify that your renamed card ("John Doe") kept its new name and the deleted connection is still gone.
  </action>
  <verify>
    <automated>MISSING — interaction/context-menu logic requires human testing</automated>
    <manual>Open index.html in browser and verify all 3 steps listed above</manual>
  </verify>
  <done>User types "approved" confirming all checks pass. Phase 9 is complete.</done>
  <resume-signal>Type "approved" if renaming and deletion work correctly. Describe any issues found if anything needs fixing.</resume-signal>
</task>

</tasks>

<verification>
Human interaction testing as described in the checkpoint task above.
</verification>

<success_criteria>
User approves the management tools. All 3 verification steps pass. Phase 9 is complete.
</success_criteria>

<output>
After user approval, create `.planning/phases/09-investigation-management/09-02-SUMMARY.md` with:
- Confirmation that interaction verification passed
- Phase 9 marked complete
</output>
