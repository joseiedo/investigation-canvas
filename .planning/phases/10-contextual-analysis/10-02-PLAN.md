---
phase: 10-contextual-analysis
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified: []
autonomous: false
requirements:
  - ANNO-04
  - ANNO-05
  - ANNO-06

must_haves:
  truths:
    - "User has verified that image cards support Red, Blue, and Black markers"
    - "User has verified that text cards allow for persistent yellow highlighting"
    - "User has verified that the toolbar adapts based on the card type"
    - "User has verified that all annotations persist after refresh and mirror to the board"
  artifacts:
    - path: "index.html"
      provides: "Verified contextual analysis tools — confirmed by human interaction"
---

<objective>
Human interaction verification of multi-color markers and text highlighting.

Purpose: Verifying the adaptive UI and the persistent "markup" feel across different media types is the final step for Milestone v3.
Output: Confirmed Phase 10 complete — Milestone v3 finished.
</objective>

<execution_context>
@/Users/iedo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iedo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-contextual-analysis/10-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Interaction verification of contextual analysis tools</name>
  <what-built>
Plan 01 implemented adaptive analysis tools in index.html with:
- Red, Blue, and Black marker buttons for image cards
- Yellow "Highlight" button for text cards
- Text selection wrapping for persistent highlights
- Real-time mirroring of highlights to board cards
- LocalStorage persistence for all contextual markup
  </what-built>
  <action>
Open index.html in your browser and perform this final verification:

1. **Multi-color Markers (Image)** — Open an image card (e.g., "Suspect A").
   - Draw something in **Red**.
   - Switch to **Blue** and draw something else.
   - Switch to **Black** and draw a third mark.
   - Verify all colors appear and mirror correctly to the board.

2. **Adaptive Toolbar (Text)** — Open the "Alibi" card.
   - Verify that the marker buttons are GONE and a "Highlight" button is visible.
   - Try to draw on the card; verify the drawing canvas is disabled (you should be able to select text instead).

3. **Text Highlighting** — 
   - Select a few words in the alibi text using your mouse.
   - Click the **Highlight** button.
   - Verify the text turns yellow.
   - Close the modal and verify the highlight appears on the board card.

4. **Persistence** — Refresh the page.
   - Verify the multi-color drawing on the image card is still there.
   - Verify the highlight on the alibi card is still there.
  </action>
  <verify>
    <automated>MISSING — contextual UI and text selection require human testing</automated>
    <manual>Open index.html in browser and verify all 4 steps listed above</manual>
  </verify>
  <done>User types "approved" confirming all checks pass. Phase 10 is complete. Milestone v3 is finished.</done>
  <resume-signal>Type "approved" if the contextual tools work correctly. Describe any issues found if anything needs fixing.</resume-signal>
</task>

</tasks>

<verification>
Human interaction testing as described in the checkpoint task above.
</verification>

<success_criteria>
User approves the contextual tools. All 4 verification steps pass. Milestone v3 is complete.
</success_criteria>

<output>
After user approval, create `.planning/phases/10-contextual-analysis/10-02-SUMMARY.md` with:
- Confirmation that interaction verification passed
- Phase 10 marked complete
- Announcement of PoC completion (v1, v2, and v3)
</output>
