---
phase: 02-card-drag
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified: []
autonomous: false
requirements:
  - DRAG-01
  - DRAG-02
  - DRAG-03

must_haves:
  truths:
    - "User has verified that all 5 cards can be dragged anywhere on the board"
    - "User has verified that the cursor changes to 'grab' on hover and 'grabbing' during drag"
    - "User has verified that the dragged card sits on top of all other cards"
  artifacts:
    - path: "index.html"
      provides: "Verified interactive drag-and-drop board — confirmed by human interaction"
---

<objective>
Human interaction verification of the card drag-and-drop functionality.

Purpose: Smoothness, coordinate accuracy, and "tactile feel" can only be truly confirmed by human interaction. This checkpoint ensures the core interactive foundation is solid before building Phase 3 (connections).
Output: Confirmed Phase 2 complete — or issues reported for gap closure.
</objective>

<execution_context>
@/Users/iedo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iedo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-card-drag/02-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Interaction verification of card dragging</name>
  <what-built>
Plan 01 implemented interactive card dragging in index.html with:
- `grab` and `grabbing` cursor feedback
- Pixels-based movement with offset calculation (no snapping)
- `z-index` management to keep the active card on top
- Viewport boundary constraints
- Deeper shadows while dragging for visual "lift"
  </what-built>
  <action>
Open index.html in your browser and verify all 5 interaction checks:

1. **Grab Cursor** — Hovering over any card shows the `grab` (open hand) cursor.

2. **Grabbing Cursor** — Clicking and holding a card changes the cursor to `grabbing` (closed fist).

3. **Smooth Dragging** — Move a card around. It should follow the cursor precisely without any "snapping" or delay.

4. **Fast Movement** — Move the mouse quickly while dragging. The card should never "slip" or detach from the cursor.

5. **Z-Index Elevation** — Drag a card over another. The card you are currently dragging must always be on top. When you release it, it should stay on top of any cards it was previously behind.

6. **Boundary Constraints** — Try to drag a card off the screen. It should stop at the edges of the cork board.

7. **Rotation Preservation** — Verify that the cards remain at their tilted angles before, during, and after the drag.
  </action>
  <verify>
    <automated>MISSING — interaction feel cannot be automated; human testing required</automated>
    <manual>Open index.html in browser and verify all 7 interaction checks listed above</manual>
  </verify>
  <done>User types "approved" confirming all 7 interaction checks pass. Phase 2 is complete.</done>
  <resume-signal>Type "approved" if the dragging feels correct. Describe any specific issues (e.g., "snapping to center", "slipping during fast movement", "can drag off-screen") if anything needs fixing.</resume-signal>
</task>

</tasks>

<verification>
Human interaction testing as described in the checkpoint task above.
</verification>

<success_criteria>
User approves the interaction. All 7 interaction checks pass. Phase 2 is complete.
</success_criteria>

<output>
After user approval, create `.planning/phases/02-card-drag/02-02-SUMMARY.md` with:
- Confirmation that interaction verification passed
- Phase 2 marked complete
</output>
