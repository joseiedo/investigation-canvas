---
phase: 07-full-persistence
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified: []
autonomous: false
requirements:
  - PERS-01
  - PERS-02
  - PERS-03
  - PERS-04

must_haves:
  truths:
    - "User has verified that all board state (positions, connections, drawings, notes) is persistent"
    - "User has verified that the board restores correctly after a page refresh"
    - "User has verified that 'Reset Board' clears everything and reloads to defaults"
  artifacts:
    - path: "index.html"
      provides: "Verified full board persistence — confirmed by human interaction"
---

<objective>
Human interaction verification of full board persistence and reset functionality.

Purpose: Verifying that the complex interplay of positions, connections, and drawings restores perfectly after a refresh is the final hurdle for the v2 PoC.
Output: Confirmed Phase 7 complete — Milestone v2 complete.
</objective>

<execution_context>
@/Users/iedo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iedo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-full-persistence/07-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Interaction verification of full board persistence</name>
  <what-built>
Plan 01 implemented full persistence in index.html with:
- Position persistence for all 5 cards
- Connection persistence for all red strings
- "Reset Board" button (bottom-right)
- Consolidated localStorage management
  </what-built>
  <action>
Open index.html in your browser and perform this comprehensive verification:

1. **Setup State** — 
   - Move several cards to new positions.
   - Enter "Connect Mode" and draw 2-3 red strings.
   - Open a card, draw a circle on it, and type a note.

2. **Verify Persistence** — 
   - Refresh the page (Cmd+R or F5).
   - Check if cards are still in their new positions.
   - Check if red strings are still there and tracking the cards.
   - Open the card you marked up and verify the drawing and note are still there.

3. **Verify Reset** — 
   - Click the "Reset Board" button at the bottom-right.
   - Confirm the dialog.
   - Verify the board returns to its original hard-coded layout, all strings disappear, and all annotations are gone.
  </action>
  <verify>
    <automated>MISSING — full persistence state requires human testing</automated>
    <manual>Open index.html in browser and verify all 3 steps listed above</manual>
  </verify>
  <done>User types "approved" confirming all persistence checks pass. Phase 7 is complete. Milestone v2 is finished.</done>
  <resume-signal>Type "approved" if everything persists and resets correctly. Describe any issues found if anything needs fixing.</resume-signal>
</task>

</tasks>

<verification>
Human interaction testing as described in the checkpoint task above.
</verification>

<success_criteria>
User approves the persistence. All 3 verification steps pass. Milestone v2 is complete.
</success_criteria>

<output>
After user approval, create `.planning/phases/07-full-persistence/07-02-SUMMARY.md` with:
- Confirmation that persistence verification passed
- Phase 7 marked complete
- Announcement of Milestone v2 delivery
</output>
