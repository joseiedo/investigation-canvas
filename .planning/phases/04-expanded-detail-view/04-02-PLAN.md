---
phase: 04-expanded-detail-view
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified: []
autonomous: false
requirements:
  - EXPV-01

must_haves:
  truths:
    - "User has verified that clicking a card opens the expanded detail view"
    - "User has verified that dragging a card does NOT open the detail view"
    - "User has verified that the detail view correctly displays the card title"
    - "User has verified that the detail view is dismissible via Escape and 'X'"
    - "User has verified that the detail view is disabled in 'Connect Mode'"
  artifacts:
    - path: "index.html"
      provides: "Verified expanded evidence view — confirmed by human interaction"
---

<objective>
Final human interaction verification of the expanded detail view.

Purpose: Distinguishing click from drag and verifying the "look closer" immersive feel (backdrop blur) requires human interaction.
Output: Confirmed Phase 4 complete — PoC complete.
</objective>

<execution_context>
@/Users/iedo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iedo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-expanded-detail-view/04-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Interaction verification of expanded detail view</name>
  <what-built>
Plan 01 implemented the expanded detail view in index.html with:
- Native HTML <dialog> element
- Immersive blurred backdrop (backdrop-filter)
- 5px movement threshold to distinguish click from drag
- Logic to prevent modal opening during "Connect Mode"
  </what-built>
  <action>
Open index.html in your browser and verify all 6 interaction checks:

1. **Click to Open** — Click (don't drag) any card. The expanded detail view should open, filling the screen with a blurred background.

2. **Correct Title** — Verify that the large title inside the modal matches the title of the card you clicked.

3. **Drag-to-Move** — Drag a card across the board. The modal should NOT open while or after dragging.

4. **Dismiss (Button)** — Click the "X" button in the top-right of the modal. It should close, returning you to the board.

5. **Dismiss (Escape)** — Click a card to open the modal again. Press the Escape key. It should close immediately.

6. **Connect Mode Exclusion** — Toggle "Connect Mode: ON". Click a card. The modal should NOT open (this allows you to select cards for connecting without interference).
  </action>
  <verify>
    <automated>MISSING — interaction feel cannot be automated; human testing required</automated>
    <manual>Open index.html in browser and verify all 6 interaction checks listed above</manual>
  </verify>
  <done>User types "approved" confirming all 6 interaction checks pass. Phase 4 is complete. PoC is finished.</done>
  <resume-signal>Type "approved" if the expanded view feels correct. Describe any issues found if anything needs fixing.</resume-signal>
</task>

</tasks>

<verification>
Human interaction testing as described in the checkpoint task above.
</verification>

<success_criteria>
User approves the interaction. All 6 interaction checks pass. Phase 4 is complete.
</success_criteria>

<output>
After user approval, create `.planning/phases/04-expanded-detail-view/04-02-SUMMARY.md` with:
- Confirmation that interaction verification passed
- Phase 4 marked complete
- Final PoC delivery announcement
</output>
