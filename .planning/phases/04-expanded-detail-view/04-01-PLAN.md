---
phase: 04-expanded-detail-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - EXPV-01

must_haves:
  truths:
    - "Clicking a card opens an expanded detail view (native dialog)"
    - "Dragging a card does NOT open the detail view"
    - "The detail view displays the card's title in a large, readable format"
    - "The detail view is dismissible via an 'X' button or the Escape key"
    - "The detail view does NOT open when in 'Connect Mode'"
  artifacts:
    - path: "index.html"
      provides: "Native <dialog> element for expanded card view"
      contains: "<dialog"
    - path: "index.html"
      provides: "Click-vs-drag detection logic (5px threshold)"
      contains: "Math.hypot"
    - path: "index.html"
      provides: "Immersive backdrop styling for the dialog"
      contains: "backdrop-filter"
---

<objective>
Implement the expanded detail view for evidence cards using a native HTML dialog with click-vs-drag detection.

Purpose: Deliver the final core capability of the PoC — the ability to "look closer" at evidence. This adds depth to the tactile experience and validates the distinction between board-level organization and evidence-level investigation.
Output: Updated index.html with a styled <dialog> and click-vs-drag interaction logic.
</objective>

<execution_context>
@/Users/iedo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iedo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-expanded-detail-view/04-CONTEXT.md
@.planning/phases/04-expanded-detail-view/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add <dialog> and styles to index.html</name>
  <files>index.html</files>
  <action>
1. Add `<dialog id="card-detail">` with a title and close button before the closing `</body>` tag.
2. Add CSS for the dialog and its backdrop.

**HTML to add:**
```html
  <dialog id="card-detail">
    <button id="close-dialog">&times;</button>
    <div class="dialog-content">
      <h1 id="dialog-title"></h1>
      <p>Investigation Evidence</p>
    </div>
  </dialog>
```

**CSS to add:**
```css
    dialog {
      padding: 40px;
      border: none;
      border-radius: 8px;
      background: #fdf6e3;
      box-shadow: 0 20px 50px rgba(0,0,0,0.5);
      max-width: 500px;
      width: 90%;
      font-family: 'Courier New', Courier, monospace;
    }

    dialog::backdrop {
      background: rgba(0, 0, 0, 0.8);
      backdrop-filter: blur(4px);
    }

    #close-dialog {
      position: absolute;
      top: 10px;
      right: 15px;
      background: none;
      border: none;
      font-size: 24px;
      cursor: pointer;
      color: #922b21;
    }

    .dialog-content h1 {
      text-transform: uppercase;
      margin-bottom: 20px;
      border-bottom: 2px solid #2a1f14;
      padding-bottom: 10px;
    }
```
  </action>
</task>

<task type="auto">
  <name>Task 2: Implement Click-vs-Drag Logic</name>
  <files>index.html</files>
  <action>
Update the `<script>` block to track movement during `mousedown`/`mouseup` and open the dialog only on true clicks.

1. Add `startPos = { x: 0, y: 0 }` and `isActuallyDragging = false`.
2. Update `mousedown` to record `startPos`.
3. Update `onMouseMove` to set `isActuallyDragging = true` if mouse moves > 5px.
4. Update `onMouseUp` to call `openCard(card)` if `!isActuallyDragging` and `!isConnectMode`.
5. Add `openCard` function and dialog close handler.

**Movement check logic:**
```javascript
const dist = Math.hypot(e.clientX - startPos.x, e.clientY - startPos.y);
if (dist > 5) isActuallyDragging = true;
```
  </action>
</task>

</tasks>

<verification>
1. Open `index.html` and drag a card. Verify the modal does NOT open.
2. Click a card (short press without moving). Verify the modal opens with the correct title.
3. Toggle "Connect Mode: ON" and click a card. Verify the modal does NOT open.
4. Press Escape while the modal is open. Verify it closes.
5. Click the "X" button. Verify it closes.
</verification>

<success_criteria>
1. Modal opens on click, not on drag.
2. Modal displays the correct title for the clicked card.
3. Modal has a blurred backdrop for immersion.
4. Modal does not trigger in Connect Mode.
5. Modal closes via Escape and close button.
</success_criteria>

<output>
After completion, create `.planning/phases/04-expanded-detail-view/04-01-SUMMARY.md` with:
- Implementation details (click threshold, dialog usage).
- Verification results.
</output>
