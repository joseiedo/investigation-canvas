---
phase: 06-note-system
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified: []
autonomous: false
requirements:
  - ANNO-03

must_haves:
  truths:
    - "User has verified that each card has a unique note field"
    - "User has verified that typing a note auto-saves immediately"
    - "User has verified that notes survive modal closure and refresh"
  artifacts:
    - path: "index.html"
      provides: "Verified note system — confirmed by human interaction"
---

<objective>
Human interaction verification of the investigative note system.

Purpose: Verifying the "no-friction" auto-save and card-specific context is essential for the investigative user experience.
Output: Confirmed Phase 6 complete — or issues reported for gap closure.
</objective>

<execution_context>
@/Users/iedo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iedo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-note-system/06-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Interaction verification of investigative note system</name>
  <what-built>
Plan 01 implemented the note system in index.html with:
- "Investigative Notes" textarea in the expanded view
- Auto-save logic on every keystroke
- Unique note storage per card index
- LocalStorage persistence
  </what-built>
  <action>
Open index.html in your browser and verify all 4 note checks:

1. **Unique Notes** — Open Card A and type "Suspect has a visible scar." Close it. Open Card B and verify the note field is EMPTY. Type "Victim was last seen at 9 PM."

2. **Persistence (Modal)** — Close Card B and re-open it. Verify "Victim was last seen at 9 PM" is still there.

3. **Persistence (Refresh)** — Refresh the browser page. Open Card A. Verify "Suspect has a visible scar" is still there.

4. **UI Feel** — Verify the textarea is easy to use, looks like a typewriter field (monospace), and doesn't disrupt the layout of the expanded view.
  </action>
  <verify>
    <automated>MISSING — interaction feel cannot be automated; human testing required</automated>
    <manual>Open index.html in browser and verify all 4 note checks listed above</manual>
  </verify>
  <done>User types "approved" confirming all 4 note checks pass. Phase 6 is complete.</done>
  <resume-signal>Type "approved" if the notes feel correct. Describe any issues found if anything needs fixing.</resume-signal>
</task>

</tasks>

<verification>
Human interaction testing as described in the checkpoint task above.
</verification>

<success_criteria>
User approves the interaction. All 4 note checks pass. Phase 6 is complete.
</success_criteria>

<output>
After user approval, create `.planning/phases/06-note-system/06-02-SUMMARY.md` with:
- Confirmation that interaction verification passed
- Phase 6 marked complete
</output>
