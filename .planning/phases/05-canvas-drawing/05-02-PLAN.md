---
phase: 05-canvas-drawing
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified: []
autonomous: false
requirements:
  - ANNO-01
  - ANNO-02

must_haves:
  truths:
    - "User has verified that drawing in the expanded view is red and semi-transparent"
    - "User has verified that the eraser removes red marks in the expanded view"
    - "User has verified that all drawings are reflected on the cards on the main board"
    - "User has verified that drawings are unique to each card"
  artifacts:
    - path: "index.html"
      provides: "Verified canvas drawing and board sync — confirmed by human interaction"
---

<objective>
Human interaction verification of red marker drawing and real-time board sync.

Purpose: The tactile feel of a marker and the visual feedback of scaling to a smaller canvas are critical for the "lived-in board" experience.
Output: Confirmed Phase 5 complete — or issues reported for gap closure.
</objective>

<execution_context>
@/Users/iedo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iedo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-canvas-drawing/05-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Interaction verification of canvas drawing and board sync</name>
  <what-built>
Plan 01 implemented red marker and eraser drawing in index.html with:
- Drawing toolbar in the expanded modal
- Marker/eraser tool state
- Card-specific drawing storage (session persistence)
- Real-time mirroring from the modal canvas to the main board card canvas
  </what-built>
  <action>
Open index.html in your browser and verify all 5 drawing checks:

1. **Drawing Tool** — Open a card. Draw on it. It should produce a semi-transparent red line that looks like a marker stroke.

2. **Eraser Tool** — Switch to the "Eraser" tool. Draw over the red lines. They should be "wiped away" without affecting the underlying image or text.

3. **Board Mirroring** — Look at the card on the board while the modal is open (if you have enough screen space) or close the modal. Verify that the miniature version of your drawing appears exactly the same on the board card.

4. **Card Independence** — Open a different card. Draw something else. Verify that the drawings are unique to each card and don't "bleed" onto others.

5. **Session Persistence** — Close the modal, then re-open the same card. Verify that your drawing is still there in both the modal and on the board.
  </action>
  <verify>
    <automated>MISSING — interaction feel cannot be automated; human testing required</automated>
    <manual>Open index.html in browser and verify all 5 drawing checks listed above</manual>
  </verify>
  <done>User types "approved" confirming all 5 drawing checks pass. Phase 5 is complete.</done>
  <resume-signal>Type "approved" if the drawing and sync feel correct. Describe any issues found if anything needs fixing.</resume-signal>
</task>

</tasks>

<verification>
Human interaction testing as described in the checkpoint task above.
</verification>

<success_criteria>
User approves the interaction. All 5 drawing checks pass. Phase 5 is complete.
</success_criteria>

<output>
After user approval, create `.planning/phases/05-canvas-drawing/05-02-SUMMARY.md` with:
- Confirmation that interaction verification passed
- Phase 5 marked complete
</output>
